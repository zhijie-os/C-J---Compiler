#include "parser.h"

struct ast *newast(struct ast *c1){

};

struct ast *newast(struct ast *c1, struct ast *c2)
{
    struct ast *a = (struct ast *)malloc(sizeof(struct ast));

    a->num_of_children = 1;
    a->
}

struct ast *newast(struct ast *c1, struct ast *c2, struct ast *c3);

struct ast *newast(struct ast *c1, struct ast *c2, struct ast *c3, struct ast *c4);
